# Examples
# No build artifacts, dependency directories, or temporary files detected in changes
# Only HTML and JS source files were modified
# Preserving any existing relevant rules would be unnecessary as no artifacts present